/**
 * TanStack Query hooks for the /proposals API.
 *
 * TODO: implement the following hooks:
 *   useProposals(status?)    — GET /proposals
 *   useApproveProposal()     — POST /proposals/{id}/approve  (mutation)
 *   useRejectProposal()      — POST /proposals/{id}/reject   (mutation)
 */

// TODO: implement hooks
