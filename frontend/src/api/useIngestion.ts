/**
 * TanStack Query hooks for the /ingestion API.
 *
 * TODO: implement the following hooks:
 *   useIngestionRuns()       — GET /ingestion/runs
 *   useIngestionRun(id)      — GET /ingestion/runs/{id}
 *   useTriggerRun()          — POST /ingestion/run  (mutation)
 *   useIngestionBatch(id)    — GET /ingestion/batches/{id}
 */

// TODO: implement hooks
